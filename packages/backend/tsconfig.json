{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "build",
    //Needed because of the popular Express `return res.send(...)` pattern
    "noImplicitReturns": false,
  },
  "include": [
    "src",
  ],
  "exclude": [
    "../../node_modules",
    "node_modules",
    "build"
  ],
  "references": [
    {
      "path": "../common/tsconfig.json"
    },
  ]
}